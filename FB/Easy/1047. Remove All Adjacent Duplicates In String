//C++:

class Solution {
public:
    string removeDuplicates(string s) {
        string res = "";
        for (auto &ch:s)
        {
            if(res.size() && ch==res.back())    res.pop_back();    
            else    res += ch;        
        }
        
        return res;
    }
};
